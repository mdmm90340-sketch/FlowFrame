# Keep the completed file outside the repository and set
# FLOWFRAME_SIGNING_PROPERTIES to its absolute path.
# A root-level keystore.properties remains supported for local compatibility.
# Never commit the completed file or the keystore itself.
storeFile=path/to/your-release.jks
storePassword=replace-me
keyAlias=replace-me
keyPassword=replace-me
