# 更新记录

## 1.1.2

- 将任务卡片右上角的视觉占位按钮替换为真正的操作菜单。
- 已完成和失败任务可选择“删除记录”，并在二次确认中明确说明不会删除相册或媒体库中的文件。
- 排队、解析、下载、合并和暂停中的任务可从同一菜单取消。
- 删除后任务列表会立即更新；保留失败任务原有的快捷删除与重试按钮。
- Android 16 arm64 真机已验证：删除确认后任务记录消失，对应的 1080×1920 媒体文件仍保留在系统媒体库。

## 1.1.1

- 修复正式包启用 R8 后 Apache Commons Compress 的反射构造器被移除，导致所有平台均提示“暂时无法解析”的问题。
- 为 `ZipExtraField` 实现类增加精准保留规则，继续保留代码压缩与资源压缩，不扩大应用权限或联网范围。
- 在 Android 16 arm64 真机以冷启动方式回归普通抖音、抖音图文和两条 B 站链接，四条均成功进入解析预览。

## 1.1.0

- 新增抖音图文作品下载：全部 WebP、原始 MP3、1080×1920 H.264/AAC MP4 三选一，默认 MP4。
- MP4 使用柔化背景完整保留画面，按音频节拍附近切图并使用 250ms 淡化；无音频时每张静音展示 3 秒。
- 新增统一链接净化：粘贴、长按粘贴和系统分享均会提取唯一链接；多个链接不猜测，输入上限 16 KiB。
- 图片在全部下载并验证后批量发布，失败或取消会回滚并清理暂存文件；图片任务支持打开首张和多图分享。
- 修复竖屏画质误降：推荐按短边选择 1080 档、节省空间选择 720 档，并正确显示方向与完整尺寸。
- 任务模型兼容 1.0.1 单输出记录，新增图文类型、输出模式和多输出位置。
- 固定内核新增 Douyin `/note/` 图文元数据支持：按原顺序提供无水印图片全部镜像、可选原声音频、音频时长和节拍时间点。
- 图文元数据采用稳定的 `flowframe_gallery` v1 JSON 结构，不复制含水印的 `download_url_list`；普通 `/video/` 解析结果保持不变。
- 内核升级为 `2026.08.19-flowframe.douyin.73d89b8.3`，并保留固定提交、三个补丁哈希与逐字节可复现构建。

## 1.0.1

- 将 APK 内置解析内核固定为 yt-dlp `2026.08.19` 加经审计的 Douyin `a_bogus` 补丁，修复公开抖音短链误报“需要登录”的问题。
- 加入内核版本 marker 与 SHA-256 校验，升级安装会可靠覆盖 youtubedl-android 留下的旧 `2025.11.12` 内核。
- 加入 FlowFrame 本地兼容补丁：Douyin 作者优先显示昵称，并为媒体请求携带一致的 User-Agent 与 Referer，修复 CDN 403。
- 加入固定提交、补丁哈希、许可证来源、专项安全审计与可复现内核构建脚本。
- 解析与下载失败统一分类并输出脱敏诊断，不再把解析器失效误报成登录限制。
- 使用用户提供的抖音短链在 Android 16 x86_64 模拟器完成覆盖升级、解析与真实下载回归。

## 1.0.0

- 首个 Android 版本。
- 支持抖音和哔哩哔哩公开单视频解析。
- 提供三种视频预设与仅音频下载。
- 加入后台队列、并发控制、进度通知、取消、重试、系统媒体库保存与分享。
- 加入链接白名单、命令注入防护、明文网络禁用和最小权限清单。
- 完成深浅主题、动态配色与 Material 3 高级界面。
